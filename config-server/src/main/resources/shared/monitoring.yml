server:
  port: 8182
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true
      
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8181/eureka/